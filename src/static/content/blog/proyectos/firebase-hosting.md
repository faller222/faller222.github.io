# Firebase Hosting

Contenido de ejemplo en subcarpeta `proyectos/`.

Si movés un post a una carpeta, cambia su URL:

- Antes: `/blog/firebase`
- Después: `/blog/proyectos/firebase`

El sidebar detecta la jerarquía automáticamente desde `index.json`.
